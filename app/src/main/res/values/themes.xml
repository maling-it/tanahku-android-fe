<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TanahKu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/poppins_medium</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:statusBarColor">@color/green</item>
        <item name="bgToolbar">@color/green</item>
        <item name="bgButton">@color/white</item>
        <item name="bgIcon">@color/black</item>
        <item name="bgItem">@color/light_gray</item>
    </style>
</resources>